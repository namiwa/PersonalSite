import * as React from 'react';

import Projects from '../components/routes/projects';

const ProjectPage = () => {
  return (
    <React.StrictMode>
      <Projects />
    </React.StrictMode>
  );
};

export default ProjectPage;
