import * as React from 'react';
import App from '../components/app';

const IndexPage = () => {
  return (
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
};

export default IndexPage;
